services:
  web:
    image: nginx:stable-alpine
    container_name: clicker_staging_web
    ports:
      - "8081:80"
    volumes:
      - ../../dist:/usr/share/nginx/html:ro
      - ../../nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
